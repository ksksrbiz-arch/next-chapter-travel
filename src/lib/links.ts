/**
 * Shared external links. Kept in one place so the agent-application CTA can be
 * updated once and flow to the header, footer, home band, and the /join page.
 */

/**
 * New-agent application form (Typeform). Jess and Wendy review every
 * submission. Tracking query params are intentionally stripped — link to the
 * clean form URL so it works from anywhere on the site.
 */
export const AGENT_APPLICATION_URL = "https://form.typeform.com/to/BrcCMxGh";
